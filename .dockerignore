__pycache__
*.pyc
.env
venv/
.venv/
.git/
# We want to mount chroma_db and docs as volumes, so we don't necessarily need to ignore them, 
# but it's good practice to not bake the DB into the image.
chroma_db/
